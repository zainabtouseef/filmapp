// The primary gold gradient CTA button (e.g. "Request Booking").
//
// Implementation lives in featured_talent_card.dart alongside its
// tightly-coupled sibling `DarkOutlineButton` and shared internals
// (`_PressableButton`, `_ButtonContent`) — re-exported here so it can
// be imported from its documented shared-widget path without
// duplicating or risking that hand-tuned implementation.
export 'featured_talent_card.dart' show GoldGradientButton;
